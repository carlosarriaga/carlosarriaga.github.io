(function($){
	$(function(){

		$('.scrollspy').scrollSpy();

		$('.parallax').parallax();

		$('.tooltipped').tooltip({delay: 50});

	}); // end of document ready
})(jQuery); // end of jQuery name space
